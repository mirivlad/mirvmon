{% extends "layout.twig" %}
{% set page_title = website.name %}
{% block content %}
<header class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4">
    <div><h1 class="h2 mb-1"><i class="fas fa-globe" aria-hidden="true"></i> {{ website.name }}</h1><p class="text-muted mb-0">{{ website.description|default('') }}</p></div>
    {% if session.role == 'admin' %}<a class="btn btn-outline-primary" href="/sites/{{ website.id }}/edit">{{ t('common.edit') }}</a>{% endif %}
</header>
<nav class="nav nav-tabs mb-4" aria-label="{{ t('websites.detail.tabs') }}">
    {% for tab in ['overview', 'metrics', 'events', 'settings'] %}<a class="nav-link {% if active_tab == tab %}active{% endif %}" href="/sites/{{ website.id }}?tab={{ tab }}">{{ t('websites.detail.tab.' ~ tab) }}</a>{% endfor %}
</nav>
{% if active_tab == 'overview' %}<div data-live-fragment="website-overview" data-live-interval-ms="10000">{% include 'sites/partials/detail-overview.twig' %}</div>{% endif %}
{% if active_tab == 'metrics' %}{% include 'sites/partials/detail-metrics.twig' %}{% endif %}
{% if active_tab == 'events' %}<div data-live-fragment="website-events" data-live-interval-ms="10000">{% include 'sites/partials/detail-events.twig' %}</div>{% endif %}
{% if active_tab == 'settings' %}{% include 'sites/partials/detail-settings.twig' %}{% endif %}
{% if active_tab == 'metrics' %}<script src="/vendor/chart.js/chart.umd.js" defer></script>{% endif %}
<script src="/js/site-detail.js" defer></script>
{% endblock %}
